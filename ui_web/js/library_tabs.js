/**
 * Library tab config: single source of truth for Library view tabs (Songs, Artists, future: Albums, Genres).
 * Used by both desktop and mobile for tab bar markup and render branching.
 */
export const LIBRARY_TABS = [
    { id: 'songs', label: 'Songs', icon: 'fa-box' },
    { id: 'artists', label: 'Artists', icon: 'fa-user' }
];
